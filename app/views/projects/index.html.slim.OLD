h1 Проекты

table.table.table-condensed
  thead
    tr
      th Наименование
      th Задачи
      th Совещания
      th Проект-менеджер
      th Статус
      th Создан
      th &nbsp;
  tbody
    = render @projects

= link_to 'Новый проект', new_project_path, class: 'btn btn-success'
= will_paginate @projects, renderer: BootstrapPagination::Rails